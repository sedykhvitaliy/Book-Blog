<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/edit.css" />
  
  <title>Editing <%= book.name %></title>
     </head>
       <body>

  <div class="box">
  <h1>Edit <%= book.name %> </h1>
  <form action="/users/<%= user._id %>/books/<%= book._id %>?_method=PUT" method="POST">

    <div class="inputBox">
    <input 
      type="text" name="name" id="name" value="<%= book.name %>" required="required" >
     <span>Name</span>
     <i></i>
     </div>  

   <div class="inputBox">
   <input type="text" name="descriptions" id="description" required="required" value="<%= book.descriptions %>">
   <span>Descriptions</span>
   <i></i>
   </div> 
   
   <div class="inputBox">
    <input type="text" name="author" id="author" required="required" value="<%= book.author %>">
    <span>Author</span>
    <i></i>
    </div> 

    <div class="inputBox">
      <input type="text" name="year" id="year" required="required" value="<%= book.year %>">
      <span>Year</span>
      <i></i>
      </div> 
     
      <a href="/users/<%= user._id %>/books">Go Back</a>
    <input type="submit" value="update">
  </form>
  </div>
</body>
</html>